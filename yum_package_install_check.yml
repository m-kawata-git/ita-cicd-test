- name: install the latest version of packages trcli
  yum:
    name: "{{ item }}"
    state: latest
  with_items:
    - "{{ VAR_packages }}"

- name: Check yum list trcli
  shell: yum list installed | grep "{{ item }}"
  register: result
  with_items:
    - "{{ VAR_packages }}"
