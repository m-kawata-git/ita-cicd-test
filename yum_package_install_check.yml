- name: install the latest version of packages trmod
  yum:
    name: "{{ item }}"
    state: latest
  with_items:
    - "{{ VAR_packages }}"

- name: Check yum list trmod
  shell: yum list installed | grep "{{ item }}"
  register: result
  with_items:
    - "{{ VAR_packages }}"
